// Converta a função no seguinte trecho de código em Arrow Function:
// const arr = [1, 2, 3, 4, 5];
// arr.map(function(item) {
// return item + 10;
// });

const arr = [1, 2, 3, 4, 5];
arr.map(item => {
return item + 10;
});

// console.log (arr)